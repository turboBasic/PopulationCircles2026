# The release build on demand, dispatched by `mise run release:smoke`. It declares no publish job and
# no write permission, so a run started here cannot create a tag or a Release however it ends.

name: Release smoke

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    uses: ./.github/workflows/build-binaries.yml
